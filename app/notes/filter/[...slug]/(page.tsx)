type Params = { slug: Promise<string[]> };

async function page({ params }: { params: Params }) {
  const slug = await params.slug;
  return (
    <>
      <h2>test</h2>
      <p>{slug}</p>
    </>
  );
}

export default page;
